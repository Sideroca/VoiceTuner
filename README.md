# 语音调控台 · VoiceTuner

安卓端语音合成调参台（自用）：把「调参 → 请求 → 试听」收进一个 App。
直连阿里云百炼 CosyVoice v3.5-plus 复刻音色，无任何中转服务器。

## 当前版本
**v0.4.5**：弹窗统一换肤（面板 / 标题 / 正文 / 按钮随主题；浅色主题下不再有深色底板和看不清的字）。

**v0.4.4**：语言栏改为「点击选择」搜索式多选（预置 中文/英语/日语/韩语/德语/法语/俄语/西班牙语/意大利语/葡萄牙语/泰语/越南语/印尼语）。

**v0.4.3**：音色选择后文字颜色修正（选择器文字随主题重着色）；生成按钮改纯色（原色保留蓝紫渐变，其余配色用各自主色，亮底自动配深字）。

**v0.4.2**：修复换肤背景色不生效（卡片/输入框/按钮随配色变化）；卡头重排（去掉「音色」标签，音色选择与「＋建音色」同行，下拉浮层跟随主题）。

**v0.4**：建音色支持一次选择任意多个音频文件（本地自动合并为 16k/16bit/单声道 WAV 后上传）；单个文件保持原样直传。

**v0.3**：新设置页（接口 / 主题配色 / 壁纸 / 关于）。
- 配色：64 套一键换装（现代经典 18 + 中国传统色 46），点选即换；默认「本机原色」
- 壁纸：主界面与设置页各自独立选图；遮罩浓度、卡片浓度、恢复默认（取景对位下一轮）
- 桌面名改为「玲珑调音」（应用内标题仍为「语音调控台」）

**v0.2**：调参面板（音色 / 文本 / 情绪指令 / 语速 / 音调 / 音量 / 种子 / 格式 / 高级参数）→ 生成 → 试听 / 分享 / 导出到「下载」 → 本地记录（回填参数、换种子重抽）；**建音色**——手机选音频样本（干净人声，推荐 ≤20 秒 / 最长 60 秒）→ 上传 → 自动创建复刻音色并加入音色列表（本地保存）。

## 安装（latest 直链，永远最新）
手机浏览器打开或书签收藏：

https://github.com/Sideroca/VoiceTuner/releases/latest/download/VoiceTuner.apk

点开即下最新版，覆盖安装即可（签名固定）。

## 使用
1. 打开 App → 右上角「设置」→ 粘贴 API Key（`sk-ws-…`，仅保存在本机 App 私有空间）
2. 选音色（或「＋ 建音色」上传样本创建新音色）→ 输入文本与情绪指令 → 生成

## 构建
- 工具链：AGP 8.7.3 / Kotlin 2.0.21 / Gradle 8.9 / JDK 17 / compileSdk 35
- 签名：固定 `debug.keystore` 随仓库保存（任何环境构建的 APK 均可互相覆盖安装）
- CI：push 后 GitHub Actions 自动编译 → Artifacts 下载 / latest 直链

## 红线
- 代码零预置密钥；API Key 由用户自行填写、仅存本机
